#include <stdio.h>
#include "variadic_functions.h"
/**
 * sum_them_all - sums them all
 * @n: the integer
 * Return: always 0
 */
int sum_them_all(const unsigned int n, ...);
{
	int = sum;

	sum = sum_them_all(4, 5, 6, 7);
	printf("%d\n", sum);
	sum = sum_them_all(5, 8, 9, 3);
	printf("%d\n", sum);

	return (0);
}
